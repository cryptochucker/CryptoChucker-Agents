Runnable Streamlit UX mockup (synthetic data). Reference for the real dashboard.
Run: python -m venv .venv && .venv/Scripts/python -m pip install streamlit plotly pandas numpy && .venv/Scripts/python -m streamlit run app.py
